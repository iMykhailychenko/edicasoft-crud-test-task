import React from 'react';
import Navbar from './nav/Navbar';

const Header: React.FC<{}> = () => (
    <header className="container my-4 px-0">
        <Navbar />
    </header>
);

export default Header;
